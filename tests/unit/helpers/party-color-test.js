import { partyColor } from 'sunlight-api/helpers/party-color';
import { module, test } from 'qunit';

module('Unit | Helper | party color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = partyColor([42]);
  assert.ok(result);
});
